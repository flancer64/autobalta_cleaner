/**
 * Plugin constants (hardcoded configuration) for shared code.
 */
export default class Ab_Clean_Back_Defaults {
    NAME = '@flancer32/lp-base';
    CLI_PREFIX = 'app';

    constructor() {
        Object.freeze(this);
    }
}
